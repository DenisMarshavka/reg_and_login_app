import {StyleSheet} from 'react-native';
import {COLORS} from "../../../constants";

export default StyleSheet.create({
    element: {
        marginTop: 15,
        marginLeft: 'auto',
        marginRight: 'auto',

        width: '100%',
        color: COLORS.error,
        textAlign: 'left',
    },
});
