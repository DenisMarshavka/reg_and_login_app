const initState = {
    globalUser: {},
    isAuthorized: true,
};

const user = (state = initState, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default user;
